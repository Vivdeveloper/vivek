# Environment secrets (never commit)
config/config.local.php
config/config.production.php

# This CMS uses MySQL only — ignore stray SQLite files if recreated by mistake
/database.sqlite
/database.db
